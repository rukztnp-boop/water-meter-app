# 🔥 Roboflow AI Integration สำหรับอ่านมาตรวัดน้ำ

ระบบได้รวม **Roboflow YOLO Object Detection** เพื่อให้อ่านค่ามาตรวัดน้ำได้แม่นยำขึ้น!

## 🎯 ความแตกต่างจาก OCR ธรรมดา

### OCR แบบเดิม (Google Cloud Vision)
- อ่านข้อความทั้งภาพ → ต้องกรองเลขที่ต้องการ
- ผิดพลาดง่ายกับตัวเลขบนมาตรที่มีแสงสะท้อน/มุม/สกปรก
- ไม่รู้ว่าตัวเลขไหนอยู่ตำแหน่งไหน

### Roboflow YOLO (ใหม่) ✨
- **Train มาเฉพาะมาตรวัดน้ำ** โดยเฉพาะ (dataset 4,000+ ภาพ)
- ตรวจจับ**แต่ละหลัก**พร้อม bounding box + confidence score
- เรียงลำดับตัวเลขจากซ้ายไปขวาอัตโนมัติ
- แม่นยำกว่า OCR ในสภาพแสง/มุมที่ยาก

## 📦 วิธีติดตั้ง

### 1. ติดตั้ง dependencies
```bash
pip install inference-sdk
```

หรือ
```bash
pip install -r requirements.txt
```

### 2. ตั้งค่า Roboflow API Key

#### ก. ลงทะเบียน Roboflow (ฟรี)
1. ไปที่ https://roboflow.com/
2. สมัครสมาชิก (Free plan = 10,000 requests/เดือน)
3. ไปที่ **Settings → API** คัดลอก API Key

#### ข. เพิ่ม API Key ใน Streamlit Secrets
แก้ไขไฟล์ `.streamlit/secrets.toml`:
```toml
roboflow_api_key = "YOUR_ROBOFLOW_API_KEY_HERE"
```

หรือถ้า deploy บน Streamlit Cloud:
1. ไปที่ **Settings → Secrets**
2. เพิ่มบรรทัด:
   ```
   roboflow_api_key = "YOUR_KEY"
   ```

## 🚀 วิธีใช้งาน

### แบบอัตโนมัติ (Auto-fallback)
ระบบจะ:
1. ลอง **Roboflow** ก่อน (ถ้ามี API key + confidence > 60%)
2. ถ้าล้มเหลว → **Fallback ไป Vision OCR** แบบเดิม

ไม่ต้องเปลี่ยนโค้ด! ทำงานแบบเดิม

### ปิด Roboflow (ใช้ OCR อย่างเดียว)
```python
result = ocr_process(image_bytes, config, use_roboflow=False)
```

## 🎨 Model ที่ใช้

ใช้ model: **`water-meter-monitoring/1`** 
- Train บน dataset FESB (4,000+ ภาพมาตรวัดน้ำ)
- ตรวจจับตัวเลข 0-9
- Confidence ≈ 85-95% (ดีกว่า OCR)

อ้างอิง: https://blog.roboflow.com/water-meter-reading/

## 📊 ข้อมูลที่ได้จาก Roboflow

```python
{
  "val": 12345.67,
  "score": 950,
  "method": "roboflow",
  "confidence": 0.95,
  "predictions": 7  # จำนวนหลักที่ตรวจพบ
}
```

## 🔧 Troubleshooting

### ไม่มี API Key
- ระบบจะ fallback ไป Vision OCR โดยอัตโนมัติ
- ไม่มี error message (silent fail)

### Roboflow ช้า
- Free plan: อาจช้าในช่วงเวลาจราจรหนาแน่น
- แก้ไข: upgrade เป็น paid plan หรือ self-host model

### ความแม่นยำไม่สูง
- ตรวจสอบ:
  - มีการตั้งค่า ROI (crop เฉพาะส่วนตัวเลข) หรือยัง?
  - มาตรที่ถ่ายมีแสงเพียงพอหรือไม่?
  - มุมภาพตรงหรือเอียงมาก?

## 💰 ราคา Roboflow

| Plan | Requests/เดือน | ราคา |
|------|----------------|------|
| Free | 10,000 | $0 |
| Starter | 100,000 | $49/เดือน |
| Professional | 1,000,000 | $299/เดือน |

**สำหรับระบบนี้:**
- ถ้าบันทึกวันละ 100 จุด = 3,000 requests/เดือน → **Free plan พอ**
- ถ้าบันทึกวันละ 300+ จุด → ควรใช้ Starter plan

## 🎯 Performance Comparison

| Method | Accuracy | Speed | Cost |
|--------|----------|-------|------|
| Vision OCR | ~70-80% | Fast | Pay-per-use |
| Roboflow YOLO | ~85-95% | Medium | Free tier available |
| Hybrid (Auto) | ~90%+ | Fast | Best of both |

## 🛠️ Advanced: Train โมเดลของคุณเอง

ถ้าต้องการความแม่นยำสูงสุดกับมาตรของคุณ:

1. เก็บภาพมาตรของคุณ 100-500 ภาพ
2. Annotate บน Roboflow Universe
3. Train โมเดลใหม่
4. เปลี่ยน `model_id` ในโค้ด:
   ```python
   digit_seq, confidence, preds = _roboflow_detect_digits(
       roi_bytes, 
       model_id="YOUR_WORKSPACE/YOUR_MODEL/1"
   )
   ```

---

**ทำโดย:** ปรับปรุงจาก https://blog.roboflow.com/water-meter-reading/
