# 🤖 คู่มือติดตั้งระบบประมวลผลอัตโนมัติ

## 📋 สถานการณ์การใช้งาน

```
[SCADA Server 1] ──┐
                   │ ช่างดึง Excel 3 ไฟล์
[SCADA Server 2] ──┘
                   
         ↓ Copy มาวาง
                   
[เครื่องพี่พจน์] ← เครื่องหลัก (เปิดตลอด 24/7)
    └─ C:\WaterMeter\Uploads\
       ├─ 5_2_69\                    ← Folder วันที่ 5 ก.พ. 2569 (แนะนำ!)
       │  ├─ 2026_02_05_WT_System.xlsx
       │  ├─ UF_System.xlsx         ← ชื่อเดิม (ไม่ซ้ำเพราะอยู่คนละ folder)
       │  └─ 2026_02_05_SMMT_Daily.xlsx
       │
       └─ 6_2_69\                    ← Folder วันที่ 6 ก.พ. 2569
          ├─ 2026_02_06_WT_System.xlsx
          ├─ UF_System.xlsx
          └─ 2026_02_06_SMMT_Daily.xlsx
       
         ↓ ประมวลผลอัตโนมัติ
         
[Google Sheets] ← บันทึกผลลัพธ์
```

**💡 หมายเหตุสำคัญ:**
- ระบบรองรับ **2 รูปแบบ**: Folder เดียว หรือ Folder แยกตามวัน
- **แนะนำใช้ Folder แยกตามวัน** เพื่อหลีกเลี่ยงปัญหาไฟล์ชื่อซ้ำ (เช่น UF_System.xlsx)
- ชื่อ Folder รูปแบบ: `5_2_69`, `05_02_69`, `5_2_2569` (d_m_yy หรือ dd_mm_yy)

---

## 🚀 ขั้นตอนการติดตั้ง

### 1️⃣ สร้างโฟลเดอร์บนเครื่องพี่พจน์

**รูปแบบ 1: Folder แยกตามวัน (แนะนำ! ⭐)**
```
C:\WaterMeter\
├── Uploads\
│   ├── 5_2_69\        ← วันที่ 5 ก.พ. 2569
│   ├── 6_2_69\        ← วันที่ 6 ก.พ. 2569
│   └── 7_2_69\
├── Processed\
└── Logs\
```

**รูปแบบ 2: Folder เดียว**
```
C:\WaterMeter\
├── Uploads\           ← ไฟล์ทุกวันอยู่ที่เดียว
├── Processed\
└── Logs\
```

เปิด Command Prompt (cmd) และรัน:
```cmd
mkdir C:\WaterMeter\Uploads
mkdir C:\WaterMeter\Processed
mkdir C:\WaterMeter\Logs
```

**⚠️ หมายเหตุสำหรับไฟล์ชื่อซ้ำ (เช่น UF_System.xlsx):**
- ถ้าใช้ **Folder แยกตามวัน** → ไม่มีปัญหา ✅
- ถ้าใช้ **Folder เดียว** → ไฟล์จะ overwrite กัน ต้องเปลี่ยนชื่อให้มีวันที่ ⚠️

---

### 2️⃣ คัดลอกไฟล์ที่จำเป็น

คัดลอกไฟล์เหล่านี้มาที่เครื่องพี่พจน์:

```
C:\water-meter-app\
├── app.py                     ← ไฟล์หลัก
├── auto_processor.py          ← ไฟล์ประมวลผลอัตโนมัติ (ใหม่)
├── DB_Water_Scada.xlsx        ← ไฟล์ mapping
├── service_account.json       ← Google credentials
└── requirements.txt           ← Python packages
```

---

### 3️⃣ ติดตั้ง Python และ Libraries

1. ติดตั้ง **Python 3.11+** จาก [python.org](https://www.python.org/downloads/)
   - ⚠️ เลือก "Add Python to PATH"

2. เปิด Command Prompt และรัน:
```cmd
cd C:\water-meter-app
pip install -r requirements.txt
```

---

### 4️⃣ ทดสอบรันครั้งแรก

```cmd
cd C:\water-meter-app
python auto_processor.py --mode manual
```

**ควรเห็น:**
```
============================================================
🚀 Manual Processing Mode
============================================================
✅ Folder ready: C:\WaterMeter\Uploads
✅ Folder ready: C:\WaterMeter\Processed
✅ Folder ready: C:\WaterMeter\Logs
🔍 Found 0 file(s) in watch folder
ℹ️ No files to process. Exiting.
```

---

## 🎯 3 โหมดการทำงาน

### โหมด 1: **Manual** (ทดสอบ/รันทันที)
```cmd
python auto_processor.py --mode manual
```
- ประมวลผลไฟล์ที่มีในโฟลเดอร์ทันที
- เหมาะสำหรับ: ทดสอบ หรือรันเองทุกครั้ง

---

### โหมด 2: **Scheduled** (แนะนำ! ⭐)
```cmd
python auto_processor.py --mode scheduled
```
- รันทุกวันเวลา **08:00** และ **16:00**
- ช่างแค่วางไฟล์ไว้ก่อน 08:00 → ระบบประมวลผลอัตโนมัติ
- **เหมาะที่สุดสำหรับการใช้งานจริง**

**ตั้งค่าเวลา:**
แก้ไขใน `auto_processor.py` บรรทัด 57:
```python
"SCHEDULED_TIMES": ["08:00", "16:00"],  # เปลี่ยนเวลาได้ตามต้องการ
```

---

### โหมด 3: **Watch** (Real-time)
```cmd
python auto_processor.py --mode watch
```
- ตรวจจับไฟล์ใหม่ทุก 5 นาที
- ประมวลผลทันทีที่มีไฟล์ใหม่
- เหมาะสำหรับ: ต้องการความเร็ว

---

## ⚙️ ตั้งค่าให้รันอัตโนมัติเมื่อเปิดเครื่อง

### 📌 วิธี 1: Task Scheduler (แนะนำ)

1. **เปิด Task Scheduler:**
   - กด `Win + R`
   - พิมพ์ `taskschd.msc` แล้วกด Enter

2. **สร้าง Task ใหม่:**
   - คลิก "Create Basic Task..."
   - Name: `Water Meter Auto Processor`
   - Description: `ประมวลผลไฟล์ Excel อัตโนมัติ`

3. **Trigger:**
   - เลือก "When the computer starts"
   - หรือ "Daily" (ถ้าต้องการรันทุกวันเวลาที่กำหนด)

4. **Action:**
   - เลือก "Start a program"
   - Program/script: `C:\Python311\python.exe`
   - Add arguments: `auto_processor.py --mode scheduled`
   - Start in: `C:\water-meter-app`

5. **Settings:**
   - ✅ Allow task to be run on demand
   - ✅ Run task as soon as possible after a scheduled start is missed
   - ✅ If the running task does not end when requested, force it to stop

6. **คลิก Finish**

---

### 📌 วิธี 2: Startup Folder (ง่ายกว่า แต่จำกัดกว่า)

1. **สร้างไฟล์ `start_auto_processor.bat`:**
```batch
@echo off
cd C:\water-meter-app
python auto_processor.py --mode scheduled
```

2. **วางไฟล์ใน Startup folder:**
   - กด `Win + R`
   - พิมพ์ `shell:startup` แล้วกด Enter
   - วางไฟล์ `.bat` ลงไป

3. **Restart เครื่อง** → Script จะรันอัตโนมัติ

---

## 📊 การใช้งานประจำวัน

### สำหรับช่าง:

**วิธีที่ 1: ใช้ Folder แยกตามวัน (แนะนำ! ⭐)**

1. **สร้าง folder วันที่** (ครั้งเดียวต่อวัน)
   ```
   C:\WaterMeter\Uploads\5_2_69\
   ```
   - รูปแบบชื่อ: `d_m_yy` (เช่น `5_2_69`, `15_2_69`)
   - หรือ `dd_mm_yy` (เช่น `05_02_69`)

2. **ดึงไฟล์จาก SCADA Server 2 เครื่อง**
   - `2026_02_05_WT_System.xlsx` (ชื่อมีวันที่)
   - `UF_System.xlsx` (ชื่อเดิม - ไม่เป็นไร!)
   - `2026_02_05_SMMT_Daily.xlsx`

3. **Copy ไฟล์ทั้งหมดไปวางใน folder วันนั้น:**
   ```
   C:\WaterMeter\Uploads\5_2_69\
   ```

4. **เสร็จ!** 🎉

---

**วิธีที่ 2: ใช้ Folder เดียว**

1. **ตรวจสอบชื่อไฟล์ให้มีวันที่ทั้งหมด:**
   - `2026_02_05_WT_System.xlsx` ✅
   - `2026_02_05_UF_System.xlsx` ✅ (ต้องเปลี่ยนชื่อให้มีวันที่!)
   - `2026_02_05_SMMT_Daily.xlsx` ✅

2. **Copy ไฟล์มาวางที่:** `C:\WaterMeter\Uploads\`

3. **⚠️ หมายเหตุ:**
   - ต้องเปลี่ยนชื่อ `UF_System.xlsx` → `2026_02_05_UF_System.xlsx`
   - ไม่งั้นจะ overwrite ไฟล์วันก่อนหน้า

---

### ระบบจะทำงานอัตโนมัติ:
- ตรวจจับไฟล์ใหม่ (ตาม hash - ไม่ประมวลผลซ้ำ)
- ประมวลผลเวลา 08:00 หรือ 16:00 (หรือทุก 5 นาทีถ้าใช้ watch mode)
- บันทึกลง Google Sheets
- ย้ายไฟล์ไป Processed folder

---

### การเช็คผลลัพธ์:

1. **ดู Log:**
   ```
   C:\WaterMeter\Logs\auto_processor_202602.log
   ```

2. **เช็คไฟล์ที่ประมวลผลแล้ว:**
   ```
   C:\WaterMeter\Processed\20260205_080015_Daily_Report.xlsx
   ```

3. **เช็คใน Google Sheets:**
   - เปิด spreadsheet
   - ดูแถวใหม่ที่เพิ่มเข้ามา
   - ค่า Inspector = "AUTO_SYSTEM"
   - ค่า Status = "AUTO"

---

## 🔧 การปรับแต่ง Config

แก้ไขไฟล์ `auto_processor.py` บรรทัด 41-77:

```python
CONFIG = {
    # โฟลเดอร์ที่ช่างจะวางไฟล์
    "WATCH_FOLDER": r"C:\WaterMeter\Uploads",
    
    # ใช้ folder แยกตามวันหรือไม่?
    "USE_DATE_FOLDERS": True,  # ← True = หา folder "5_2_69" (แนะนำ!)
                                # False = หาใน folder เดียว
    
    # โฟลเดอร์เก็บไฟล์ที่ประมวลผลแล้ว
    "PROCESSED_FOLDER": r"C:\WaterMeter\Processed",
    
    # เวลาที่ต้องการประมวลผล
    "SCHEDULED_TIMES": ["08:00", "16:00"],  # ← เปลี่ยนได้
    
    # Check interval (วินาที) สำหรับ watch mode
    "WATCH_INTERVAL": 300,  # ← 300 = 5 นาที
    
    # จำนวนแถวที่สแกน
    "MAX_SCAN_ROWS": 50000,  # ← เพิ่มได้ถ้าไฟล์ใหญ่
}
```

**การตั้งค่าสำคัญ:**
- `USE_DATE_FOLDERS = True` → หา folder `5_2_69`, `6_2_69` (แนะนำ!)
- `USE_DATE_FOLDERS = False` → หาในโฟลเดอร์เดียว (ต้องระวังชื่อซ้ำ)

---

## ❓ Troubleshooting

### ปัญหา: ไฟล์ไม่ถูกประมวลผล

**เช็ค:**
1. ไฟล์อยู่ในโฟลเดอร์ `C:\WaterMeter\Uploads\` หรือไม่?
2. ชื่อไฟล์ตรงกับ pattern หรือไม่? (ต้องมี `Daily_Report`, `UF_System`, หรือ `SMMT`)
3. Script รันอยู่หรือไม่? (เช็คใน Task Manager)
4. ดู log: `C:\WaterMeter\Logs\auto_processor_*.log`

---

### ปัญหา: Google Sheets ไม่อัพเดท

**เช็ค:**
1. ไฟล์ `service_account.json` อยู่ในโฟลเดอร์หรือไม่?
2. Internet เชื่อมต่อหรือไม่?
3. Google Sheets URL ถูกต้องในไฟล์ `app.py` หรือไม่?
4. Service Account มีสิทธิ์ Editor ใน Google Sheets หรือไม่?

---

### ปัญหา: Script หยุดทำงาน

**แก้ไข:**
1. เช็ค log หา error
2. Restart Task Scheduler task
3. หรือรันด้วย Command Prompt เพื่อดู error โดยตรง:
   ```cmd
   cd C:\water-meter-app
   python auto_processor.py --mode scheduled
   ```

---

## 📞 ติดต่อ / Support

หากมีปัญหาหรือต้องการความช่วยเหลือ:
- ดู log file: `C:\WaterMeter\Logs\`
- แจ้งพร้อม error message จาก log

---

## ✅ Checklist ก่อนใช้งานจริง

- [ ] Python 3.11+ ติดตั้งแล้ว
- [ ] `pip install -r requirements.txt` สำเร็จ
- [ ] โฟลเดอร์ `C:\WaterMeter\` ถูกสร้างแล้ว
- [ ] ไฟล์ `service_account.json` อยู่ในโฟลเดอร์โปรเจค
- [ ] ไฟล์ `DB_Water_Scada.xlsx` อยู่ในโฟลเดอร์โปรเจค
- [ ] ทดสอบรัน `--mode manual` สำเร็จ
- [ ] Task Scheduler ตั้งค่าแล้ว (ถ้าใช้)
- [ ] ทดสอบวางไฟล์ครั้งแรกและเช็คว่าประมวลผลสำเร็จ

---

## 🎉 พร้อมใช้งาน!

ตอนนี้ระบบพร้อมทำงานอัตโนมัติแล้ว:

1. ✅ ช่างวางไฟล์ → `C:\WaterMeter\Uploads\`
2. ✅ ระบบประมวลผลอัตโนมัติ (08:00 และ 16:00)
3. ✅ บันทึกลง Google Sheets
4. ✅ ย้ายไฟล์ไป `Processed\`
5. ✅ บันทึก log สำหรับตรวจสอบ

**ไม่ต้องเปิด Web App เลย!** 🚀
